// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyDamageArgs {
    pub target_id: __sdk::Uuid,
    pub amount: u32,
}

impl From<ApplyDamageArgs> for super::Reducer {
    fn from(args: ApplyDamageArgs) -> Self {
        Self::ApplyDamage {
            target_id: args.target_id,
            amount: args.amount,
}
}
}

impl __sdk::InModule for ApplyDamageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_damage`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_damage {
    /// Request that the remote module invoke the reducer `apply_damage` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_damage:apply_damage_then`] to run a callback after the reducer completes.
    fn apply_damage(&self, target_id: __sdk::Uuid,
amount: u32,
) -> __sdk::Result<()> {
        self.apply_damage_then(target_id, amount,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `apply_damage` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_damage_then(
        &self,
        target_id: __sdk::Uuid,
amount: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_damage for super::RemoteReducers {
    fn apply_damage_then(
        &self,
        target_id: __sdk::Uuid,
amount: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(ApplyDamageArgs { target_id, amount,  }, callback)
    }
}

