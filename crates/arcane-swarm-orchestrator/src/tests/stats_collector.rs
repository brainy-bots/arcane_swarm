#[test]
#[ignore]
fn collector_reports_correct_rates_and_counters() {
    // Acceptance: Mock cluster server emits known stats; collector reports correct rates and counters.
    todo!()
}

#[test]
#[ignore]
fn polling_continues_when_cluster_unreachable() {
    // Acceptance: Polling continues when one cluster is briefly unreachable; resumes on recovery.
    todo!()
}

#[test]
#[ignore]
fn time_series_memory_bounded() {
    // Acceptance: Time-series memory bounded to 5 minutes (no unbounded growth over a 24-hour soak).
    todo!()
}
